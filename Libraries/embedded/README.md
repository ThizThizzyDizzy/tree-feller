# TreeFeller embedded libraries

**fastutil-minimal**

This is a minimal jar, containing the following classes (and their dependencies):
- Int2ObjectOpenHashMap
- Long2ObjectOpenHashMap

**config** (https://github.com/lightbend/config)

These libraries have been repackaged under `com.thizthizzydizzy.treefeller.lib` to prevent conflicts. (See the `utility scripts` folder for more details)
